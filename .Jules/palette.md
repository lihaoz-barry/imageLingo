## 2024-05-23 - Invisible Interactive Elements
**Learning:** `opacity-0` with `group-hover:opacity-100` makes elements invisible to keyboard users even when focused.
**Action:** Always add `focus:opacity-100` and `focus-visible` styles to hover-reveal interactive elements.
