## 2024-05-20 - Custom Button Accessibility
**Learning:** Custom buttons often miss standard focus states provided by the UI library.
**Action:** Audit all `button` and `a` tags not using the `Button` component for focus styles.
